import React from "react";
import ReactDOM from "react-dom/client";
import App from "../QuickPlay.jsx";

ReactDOM.createRoot(document.getElementById("qp-root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);